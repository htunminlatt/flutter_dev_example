class Trip{
  final String title;
  final List<String> photos;
  final String description;
  final DateTime data;
  final String location;

  Trip(this.title, this.photos, this.description, this.data, this.location);
  
}